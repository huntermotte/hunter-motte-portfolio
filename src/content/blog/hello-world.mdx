---
title: "Hello, world"
description: "Actual content coming soon. Writing from the heart takes time."
pubDate: 2026-05-31
tags: ["meta"]
draft: false
---

This is a placeholder post so the blog scaffolding has something to render.
Real writing comes later — this exists to prove the pipeline works end to end.

## A second heading, for typography testing

A short paragraph follows the heading. The goal here is to make sure the
**bold**, *italic*, and `inline code` treatments all read well against the
bone background, and that line length stays comfortable for long-form reading.

- A list item, to verify the bullet treatment
- Another item, slightly longer, so we can see how wrapping behaves at the
  edge of the prose column
- A third, for good measure

> A blockquote, set apart from the rest. Often the only place an EM gets to
> sound like an essayist instead of a Slack-er.

That's it. Replace this file when real writing arrives.
